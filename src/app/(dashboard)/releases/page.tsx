import { CalendarDays, GitCommitHorizontal, ArrowUpRight, History } from "lucide-react";
import packageJson from "../../../../package.json";
import notices from "@/lib/software-notices.json";
import history from "@/lib/release-history.json";
export default function ReleaseHistoryPage() {
 const changes = ["Unified trtDigital Mail identity across login, inbox, settings, and administration.", "Removed pricing pages and upgrade prompts from the internal workspace.", "Refined responsive navigation, empty folders, and mail connection status.", "Added version history with dated source commits and release notes."];
 return <div className="mx-auto max-w-5xl p-6 lg:p-10"><p className="mail-eyebrow">TRTDIGITAL MAIL / PRODUCT UPDATES</p><div className="mb-8 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl">Release history</h1><p className="mt-3 text-sm text-slate-500">What changed, when it changed, and the source behind it.</p></div><span className="rounded-lg border border-blue-200 bg-blue-50 px-3 py-2 text-xs font-semibold text-blue-700">Installed version {packageJson.version}</span></div><section className="rounded-2xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex flex-wrap items-center gap-3"><History className="text-blue-600" size={22}/><h2 className="text-xl font-semibold">{packageJson.version} · A workspace of our own</h2><span className="rounded-md bg-amber-50 px-2 py-1 text-xs text-amber-800">Release notes</span></div><p className="mt-3 flex items-center gap-2 text-xs text-slate-500"><CalendarDays size={14}/>15 September 2026</p><ul className="mt-6 space-y-3 text-sm leading-6 text-slate-600">{changes.map(change=><li key={change} className="flex gap-3"><span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-blue-500"/>{change}</li>)}</ul><p className="mt-6 border-t border-slate-100 pt-4 text-xs leading-6 text-slate-500">Version 0.3.0 includes the workspace redesign and navigation fixes. Previous release baseline: <code>{history.baseCommit.slice(0,7)}</code>.</p></section><section className="mt-8"><h2 className="text-lg font-semibold">Previous source commits</h2><p className="mt-2 text-xs text-slate-500">Repository history preceding this release.</p><div className="mt-4 overflow-hidden rounded-2xl border border-slate-200 bg-white">{history.commits.map(commit=><a key={commit.hash} href={`https://github.com/naimbic/trtMail/commit/${commit.hash}`} target="_blank" rel="noreferrer" className="flex items-start gap-4 border-b border-slate-100 p-5 last:border-0 hover:bg-slate-50"><GitCommitHorizontal size={20} className="mt-1 shrink-0 text-slate-400"/><div className="min-w-0 flex-1"><p className="text-sm font-medium">{commit.subject}</p><p className="mt-2 text-xs text-slate-500"><code>{commit.hash.slice(0,7)}</code> · <time dateTime={commit.date}>{commit.date.slice(0,10)}</time></p></div><ArrowUpRight size={16} className="shrink-0 text-slate-400"/></a>)}</div></section><section className="mt-8 rounded-2xl border border-slate-200 bg-white p-6"><h2 className="text-lg font-semibold">Software notices</h2><p className="mt-3 text-sm text-slate-600">Powered by <a href="https://mailflare.co/" target="_blank" rel="noreferrer" className="text-blue-600 underline">Mailflare</a>. Copyright (c) 2026 Hieu Nguyen.</p><details className="mt-4"><summary className="cursor-pointer text-sm font-medium">Source license</summary><pre className="mt-4 whitespace-pre-wrap text-xs leading-6 text-slate-600">{notices.license}</pre></details></section></div>;
}
