"use client";
import type { ForwardingEmailFormProps } from "./types";
export function ForwardingEmailForm(_props:ForwardingEmailFormProps){return <p className="text-sm text-neutral-500">Configure forwarding at your email provider. This server imports mail through IMAP; it does not change provider forwarding rules.</p>;}
